language: php

php:
  - 7.1
  - 7.2
  - hhvm

matrix:
  allow_failures:
    - php: hhvm

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer install

script: vendor/bin/phpunit

after_script:
  - vendor/bin/coveralls -v
